#include <stdio.h>
#include <Windows.h>

int canConstruct(char* ransomNote, char* magazine){


}

int main(){
	char ransomNote[] = { "aab" };
	char magazine[] = { "aacb" };
	int end = canConstruct(ransomNote, magazine);
	system("pause");
	return 0;
}